

# Verwijder subtitel van Assistent header

## Wijziging

In `src/pages/Assistent.tsx` de `subtitle` prop van `<PageHeader>` verwijderen of vervangen door een korte, warme regel.

Voorstel: `subtitle="Regelt het, zodat jij kunt koken"` — of helemaal weglaten.

### Bestand

| Bestand | Wijziging |
|---|---|
| `src/pages/Assistent.tsx` | `subtitle` prop verwijderen of vervangen |

